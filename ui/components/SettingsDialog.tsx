
interface openProps{
    isOpen:boolean;
     setIsOpen: (isOpen: boolean) => void;
}

const SettingsDialog = ({isOpen,setIsOpen}:openProps) => {
  return (
    <div>

    </div>
  )
}

export default SettingsDialog